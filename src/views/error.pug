extends layout

block content
  .card
    span.eyebrow Incidencia detectada
    h2 Error #{statusCode}
    p #{message}
    br
    .actions
      a.btn.btn-primary(href="/inicio") Volver al Inicio
      // a.btn.btn-warning(href="javascript:history.back()") Volver
